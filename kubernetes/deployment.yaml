apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-app-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: todo-app-10
  template:
    metadata:
      labels:
        app: todo-app-10
    spec:
      containers:
      - name: todo-app-10
        image: trainingapcacr.azurecr.io/todo-app:training10-2
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: todo-app-service-10
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
  selector:
    app: todo-app-10
